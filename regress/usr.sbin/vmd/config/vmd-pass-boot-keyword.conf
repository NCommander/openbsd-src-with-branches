#	$OpenBSD$
# Pass on boot keyword as it has replaced the kernel keyword.
ramdisk="/bsd.rd"
switch "sw" {
    add vether0
}
vm "x" {
    boot $ramdisk
    memory 1G
    disable
    interface { switch "sw" }
}
