#	$OpenBSD$

PROGDIR=	${.CURDIR}/../../../usr.sbin/traceroute

LDSTATIC=	-static
CFLAGS+=	-I${PROGDIR}
NOMAN=		yes

.include "${PROGDIR}/Makefile"

BINDIR=		/var/www/bin
BINMODE=	000

.PATH:		${PROGDIR}

